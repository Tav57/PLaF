let l1 = {head <= 0; length <= 0}
in let add_front = proc (x) {proc (l) {
    begin
        l.head <= {data <= x; next <= l.head};
        l.length <= l.length+1
    end
}}
in begin
    ((add_front 2) l1);
    ((add_front 3) l1);
    debug(l1)
   end